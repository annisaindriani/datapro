% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ind_reduksi_dimensi.R, R/reduksi_dimensi.R
\name{reduksi_dimensi}
\alias{reduksi_dimensi}
\title{Reduksi dimensi dataset menggunakan berbagai metode}
\usage{
reduksi_dimensi(
  data,
  metode = c("PCA", "ICA", "t-SNE", "UMAP", "LDA"),
  n_komponen = 2,
  target = NULL,
  scale = TRUE,
  center = TRUE,
  buat_plot = TRUE,
  warna_berdasarkan = NULL,
  judul = NULL,
  seed = 42,
  return_plotly = FALSE,
  ...
)

reduksi_dimensi(
  data,
  metode = c("PCA", "ICA", "t-SNE", "UMAP", "LDA"),
  n_komponen = 2,
  target = NULL,
  scale = TRUE,
  center = TRUE,
  buat_plot = TRUE,
  warna_berdasarkan = NULL,
  judul = NULL,
  seed = 42,
  return_plotly = FALSE,
  ...
)
}
\arguments{
\item{data}{Data frame atau matriks yang berisi data untuk direduksi.}

\item{metode}{Metode reduksi dimensi: "PCA", "ICA", "t-SNE", "UMAP", atau "LDA".}

\item{n_komponen}{Jumlah dimensi yang akan direduksi (default 2).}

\item{target}{Untuk LDA saja, variabel faktor yang berisi label kelas.}

\item{scale}{Logis, apakah data akan discale sebelum reduksi (default TRUE).}

\item{center}{Logis, apakah data akan dicenterkan sebelum reduksi (default TRUE).}

\item{buat_plot}{Logis, apakah akan membuat plot (default TRUE).}

\item{warna_berdasarkan}{Vektor atau nama kolom untuk mewarnai titik dalam plot.}

\item{judul}{Judul opsional untuk plot.}

\item{seed}{Random seed untuk reproduktibilitas (default 42).}

\item{return_plotly}{Logis, apakah mengembalikan plot interaktif plotly (default FALSE).}

\item{...}{Parameter tambahan untuk metode reduksi dimensi spesifik.}
}
\value{
List berisi data yang telah direduksi, model, plots, ringkasan, info_metode, validasi.

List berisi data yang telah direduksi, model, plots, ringkasan, info_metode, validasi.
}
\description{
Fungsi ini melakukan reduksi dimensi pada dataset menggunakan metode seperti PCA,
ICA, t-SNE, UMAP, atau LDA dan menghasilkan berbagai plot untuk memudahkan analisis statistik.

Fungsi ini melakukan reduksi dimensi pada dataset menggunakan metode seperti PCA,
ICA, t-SNE, UMAP, atau LDA dan menghasilkan berbagai plot untuk memudahkan analisis statistik.
}
\examples{
# Memuat data iris
library(datasets)
hasil_pca <- reduksi_dimensi(iris[, 1:4], metode = "PCA",
                            warna_berdasarkan = iris$Species)

# Memuat data iris
library(datasets)
hasil_pca <- reduksi_dimensi(iris[, 1:4], metode = "PCA",
                            warna_berdasarkan = iris$Species)

}
\seealso{
\code{\link{normalisasi_data}} untuk fungsi normalisasi atau penskalaan dataset.
\code{\link{transformasi_data}} untuk fungsi transformasi data.

\code{\link{normalisasi_data}} untuk fungsi normalisasi atau penskalaan dataset.
\code{\link{transformasi_data}} untuk fungsi transformasi data.
}
